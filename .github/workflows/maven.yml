name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 14-ea
      uses: actions/setup-java@v1
      with:
        java-version: '14-ea'
    - name: Build with Maven
      run: ./mvnw -B clean package
